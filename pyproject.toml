[project]
name = "rto-reco"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "customtkinter>=5.2.2",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
